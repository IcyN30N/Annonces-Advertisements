<p id="notice"><%= notice %></p>


<section class="row text-center">
  <h1 class="col-lg-12">Commentaires</h1>
  <div class="col-lg-4">
  </div>
  <%= link_to 'Nouveau Commentaire', new_comment_path, class:"perso-link col-lg-4" %>
  <div class="col-lg-4">
  </div>
</section>
<section class="row content-row">
  <% @comments.each do |comment| %>
    <div class="col-lg-4 text-center">
      <h2><%= comment.user_id %></h2>
      <p><%= comment.content %></p>
      <p><%= link_to 'détails', comment %></p>
      <p><%= link_to 'Edit', edit_comment_path(comment) %></p>
      <p><%= link_to 'Destroy', comment, method: :delete, data: { confirm: 'Are you sure?' } %></p>
    </div>
  <% end %>
</section>
